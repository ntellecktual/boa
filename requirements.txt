# ==============================================================================
# BOA - Core Dependencies
# ==============================================================================

# Django & Extensions
Django>=5.2,<5.3
django-widget-tweaks>=1.5
django-environ>=0.13
whitenoise>=6.5

# Database
psycopg2-binary>=2.9

# Task Queue (PostgreSQL-backed, no Redis needed)
celery>=5.3
django-celery-results>=2.5
kombu>=5.3

# WebSocket / Channels (real-time progress, chatbot)
channels>=4.0
daphne>=4.0
channels-redis>=4.1

# API Framework
django-ninja>=1.0

# Auth & Social Login
django-allauth[socialaccount]>=65.0

# HTMX (modern frontend)
django-htmx>=1.17

# Structured Logging
django-structlog>=10.0
structlog>=25.0

# CORS
django-cors-headers>=4.0

# Caching
django-cachalot>=2.9

# Import/Export
django-import-export>=4.0

# Error Tracking
sentry-sdk[django]>=2.0

# Jupyter Notebook Processing
nbformat>=5.10
nbconvert>=7.16
markdown>=3.6
beautifulsoup4>=4.12

# Text-to-Speech & Audio
edge-tts>=6.1

# Voice Cloning (optional — set ELEVENLABS_API_KEY in .env)
elevenlabs>=1.0

# LLM Narration (optional — leave API keys blank to use basic TTS fallback)
anthropic>=0.40
openai>=1.40

# Development tools (inactive in production when DEBUG=False)
django-debug-toolbar>=4.3

# Testing
pytest>=8.0
pytest-django>=4.9

# Linter & Formatter
ruff>=0.15

# RAG / Embeddings
chromadb>=0.4
sentence-transformers>=2.2

# Video Generation
moviepy==1.0.3
Pillow>=10.0

# Analytics & Charts (server-side rendering fallback)
numpy>=1.24

# GitHub Webhooks
cryptography>=41.0

# Production Server
gunicorn>=22.0
